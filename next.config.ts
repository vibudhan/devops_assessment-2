/** @type {import('next').NextConfig} */
const nextConfig = {
  // ADD THIS LINE:
  output: 'standalone', 
  // You may have other properties here like reactStrictMode: true
};

export default nextConfig;
